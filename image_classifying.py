import cv2 # let us get the mac version that has less features than the actual cv
from matplotlib  # optional to display frames from videos or images.
import numpy as np

# We should always try to convert the image into something else such as a grayscale, color filtering, edge detection, feature
# matching for object detection and general object recognition



